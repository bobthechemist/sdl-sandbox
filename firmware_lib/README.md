# Circuitpython lib directory

Place files here that might need to be deployed to your firmware (e.g. `adafruit_logging.mpy`). These files are not tracked. End user is responsible for including the correct version. To use `deploy.py` properly, ensure that the library is listed in **FIRMWARE_DEPENDENCIES** in `firmware_dp.py`.
