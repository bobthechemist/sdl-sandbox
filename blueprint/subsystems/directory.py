##############
# Microcontroller identification
#   
# Only the VID and PID values can be changed in `boot.py`. Using these dicts to provide meaningful values

vid = {
    808 : "Brockport Original Builds",
}

pid = {
    808 : "Pumps(TEST)",
    809 : "Sample(TEST)",
}